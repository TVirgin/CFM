import Layout from '@/components/layout';
import * as React from 'react';

interface IFreeHelpProps {
}

const FreeHelp: React.FunctionComponent<IFreeHelpProps> = (props) => {
  return (
    <Layout >
      <div>FreeHelp</div>
    </Layout>
  );
};

export default FreeHelp;
