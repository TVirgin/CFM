import Layout from '@/components/layout';
import * as React from 'react';

interface IDownloadsProps {
}

const Downloads: React.FunctionComponent<IDownloadsProps> = (props) => {
  return (
    <Layout >
      <div>Downloads</div>
    </Layout>
  );
};

export default Downloads;
